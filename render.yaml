services:
  - type: web
    name: neurotube-ai-pro
    runtime: node
    plan: free
    buildCommand: |
      pip3 install edge-tts
      npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
